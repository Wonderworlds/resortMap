# Decision Log — SPEC-resort-map

## 2026-06-16 — Initial create

**Input:** In-chat description of a Bun monorepo with 5 packages (builder-core, builder-react, view-core, view-react, view-react-native) producing and consuming a custom `.gwmap` file format.

**Pass 1 — Coherence:**
- All 8 capabilities have both `intent` and `success` fields. ✓
- Intents are WHAT-not-HOW; implementation details (Dijkstra, immutable update pattern) are deferred to `package-architecture.md`. ✓
- Constraints bend real decisions: Node 18 affects API surface (no top-level await without flag, no newer TS features), Bun monorepo affects tooling, core/adapter split prevents logic duplication, URL-only background prevents binary bloat. ✓
- Non-goals are explicit (7 items); GPS, multi-user, geospatial formats, and non-walk modes are all defined out. ✓
- Success signal is concrete and demonstrable end-to-end (builder-react → `.gwmap` → both viewers, all features exercised). ✓
- Capability IDs CAP-1 through CAP-8 assigned; none retired or reused. ✓

**Pass 2 — Preservation:**
- Background image URL → `map.backgroundImageUrl` in schema + CAP-1/CAP-2. ✓
- Center + scale → `map.center`, `map.scale` in schema + CAP-1/CAP-2. ✓
- POI (tags, icon, configurable data) → `gwmap-schema.md` POI object with `tags`, `icon`, `meta`. ✓
- Default tags (restaurant, wc) → `gwmap-schema.md` default vocabulary note. ✓
- Roads for itinerary → `graph.nodes` + `graph.edges` in schema. ✓
- Walk-time compute → CAP-4, walk-time formula in `gwmap-schema.md`. ✓
- Filter by tags + distance → CAP-5, `filterPois()` in `package-architecture.md`. ✓
- React ≥18 web viewer → CAP-6, `view-react`. ✓
- React Native mobile viewer → CAP-7, `view-react-native`. ✓
- Core/adapter split (framework-agnostic) → CAP-8, enforced in Constraints. ✓
- Node 18 compatibility → Constraint. ✓
- Bun monorepo → Constraint. ✓
- Example reference (stay-app.com/hotel-map) → used for feature parity understanding only; not a source artifact, not listed in sources. ✓

**Wrapper-only content dropped:** Phrasing about "typescript using node" (redundant once Node 18 constraint is captured). The phrase "compatible till node 18" was interpreted as a minimum compatibility floor (must work on Node 18+), recorded as an assumption.

**Assumptions added:** 7 (walk speed, scale direction, POI-to-node snapping, builder-react as standalone app, coordinate origin, tag openness, undirected-by-default edges).

**Open questions added:** 5 (POI/node namespace merge, offline RN, route animation, graph scale for algorithm choice, undo/redo in builder).

**Verdict:** Spec is coherent and preservation-complete. All load-bearing claims from the input landed in SPEC.md or a companion. The 5 open questions are non-blocking for architecture but should be resolved before implementation of CAP-4 (routing) and CAP-7 (RN offline).

## 2026-06-16 — Update: Expo constraint + visual reference

**Trigger:** User clarified that the mobile app is exported via Expo (iOS + Android), and referenced https://www.stay-app.com/hotel-map as the UX target for the viewer.

**Changes applied:**
- Added two Expo constraints to SPEC.md Constraints section: Expo managed/bare workflow compatibility, Expo-compatible gesture/animation/SVG libraries only.
- Updated open question about RN offline to reference Expo asset bundling specifically.
- Updated `package-architecture.md` view-react-native section: added explicit Expo dependency list (react-native-svg, react-native-gesture-handler, react-native-reanimated, expo-file-system), updated rendering description to SVG overlay on Image.

**Visual reference note:** The stay-app.com/hotel-map reference shows a custom image-based map with POI category filters, tap-to-select POI behavior, and A→B routing. This is consistent with all existing CAPs; no new capabilities required. The reference is adopted as a UX intent reference, not a source artifact.
